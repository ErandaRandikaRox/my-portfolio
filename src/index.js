import React from 'react';
import ReactDOM from 'react-dom';
import './styles.css'; // Updated to new CSS file
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);